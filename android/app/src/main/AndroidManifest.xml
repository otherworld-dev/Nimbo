<?xml version="1.0" encoding="utf-8"?>
<!--
    Nimbo for Android — application manifest.

    Shape of the app: one launcher Activity (Compose UI) plus one foreground
    "dataSync" Service that owns the running Go sync engine. WorkManager's
    periodic wake-up is registered in code, so it needs no manifest entry
    (androidx.startup provides its initializer).

    Permission notes:
      * MANAGE_EXTERNAL_STORAGE is genuinely required — a file-sync app has to
        write user-visible files under /storage/emulated/0/Nimbo, and the pairs
        bake in their absolute local path. It is a settings toggle, not a
        runtime dialog.
      * READ/WRITE_EXTERNAL_STORAGE are capped to the API levels where they
        still mean anything (32 / 29 respectively).
      * FOREGROUND_SERVICE_DATA_SYNC is mandatory from API 34 for the
        foregroundServiceType declared on SyncService below.

    Backup: both backup rule files exclude the `nimbo_secrets` SharedPreferences
    and the app's files/ directory. Android Keystore keys never leave the
    device, so a restored ciphertext would be permanently undecryptable, and a
    restored engine database would disagree with the (absent) local files.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Package visibility (API 30+): the Login Flow opens the server's approval
        page in a Custom Tab, and the Apps screen opens https links. Without
        this, resolving a browser returns null on Android 11+.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".NimboApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nimbo"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Nimbo"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Owns the running Go engine; started with startForegroundService(). -->
        <service
            android:name=".service.SyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
          Hands downloaded and synced files to other apps. Android refuses
          file:// URIs in intents, so opening anything at all depends on this.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
